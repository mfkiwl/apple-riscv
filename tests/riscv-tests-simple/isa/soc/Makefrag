#=======================================================================
# Makefrag for soc tests
#-----------------------------------------------------------------------

tests_name = \
	software_interrupt \
	timer_interrupt \

soc_tests = $(addprefix soc-, $(tests_name))
